{
  "name": "@0xcert/web3-dxm",
  "version": "0.0.0",
  "description": "Decentralized minter implementation for 0xcert protocol. ",
  "scripts": {
    "compile": "specron compile",
    "sandbox": "specron sandbox",
    "prepublishOnly": "specron compile",
    "test": "rm -rf build && specron compile && specron test --require ts-node/register --match ./src/tests/**/*.test.ts"
  },
  "license": "MIT",
  "devDependencies": {
    "@specron/cli": "latest",
    "@specron/spec": "latest",
    "ts-node": "^7.0.1",
    "typescript": "^3.0.1",
    "web3": "git+https://github.com/0xcert/web3.js.git#newAbiLib"
  }
}
