import { Spec } from '@specron/spec';

const spec = new Spec();

spec.test('deploy minter with correct ERC20 transfer proxy address', async (ctx) => {
  
});

spec.test('deploy minter with correct Xcert minter proxy address', async (ctx) => {
  
});

export default spec;