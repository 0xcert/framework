{
  "name": "@0xcert/web3-xcert",
  "description": "Xcert token implementation for the 0xcert protocol.",
  "version": "0.0.0",
  "scripts": {
    "compile": "specron compile",
    "sandbox": "specron sandbox",
    "prepublishOnly": "specron compile",
    "test": "rm -rf build && specron compile && specron test --require ts-node/register --match ./src/tests/**/*.test.ts"
  },
  "license": "MIT",
  "dependencies": {
    "@specron/cli": "latest",
    "@specron/spec": "latest",
    "ts-node": "^7.0.1",
    "typescript": "^3.0.1",
    "web3": "^1.0.0-beta.35"
  }
}
