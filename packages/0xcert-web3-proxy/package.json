{
  "name": "@0xcert/web3-proxy",
  "version": "0.0.0",
  "description": "Proxy implementation for 0xcert protocol. ",
  "scripts": {
    "compile": "specron compile",
    "sandbox": "specron sandbox",
    "prepublishOnly": "specron compile",
    "test": "rm -rf build && specron compile && specron test --require ts-node/register --match ./src/tests/**/*.test.ts"
  },
  "license": "MIT",
  "devDependencies": {
    "@0xcert/ethereum-xcert": "^1.8.0",
    "@0xcert/web3-erc20": "file:../0xcert-web3-erc20",
    "@0xcert/web3-erc721": "file:../0xcert-web3-erc721",
    "@specron/cli": "^0.0.42",
    "@specron/spec": "^0.0.42",
    "ts-node": "^7.0.1",
    "typescript": "^3.0.1",
    "web3": "1.0.0-beta.36"
  }
}
