{
  "name": "@0xcert/ethereum-proxy-contracts",
  "version": "0.0.0-alpha3",
  "description": "Proxy implementation for 0xcert protocol. ",
  "scripts": {
    "clean": "rm -Rf ./dist",
    "build": "npm run clean && npx specron compile",
    "sandbox": "npx specron sandbox",
    "prepare": "npm run build",
    "test": "rm -rf build && npx specron compile && npx specron test"
  },
  "specron": {
    "compiler": {
      "build": "./build",
      "match": [
        "./src/**/*.sol"
      ]
    },
    "test": {
      "port": 8504,
      "match": [
        "./src/tests/**/*.test.ts"
      ]
    },
    "require": [
      "ts-node/register"
    ]
  },
  "license": "MIT",
  "devDependencies": {
    "@0xcert/ethereum-utils-contracts": "0.0.0-alpha3",
    "@0xcert/ethereum-erc20-contracts": "0.0.0-alpha3",
    "@0xcert/ethereum-erc721-contracts": "0.0.0-alpha3",
    "@0xcert/ethereum-xcert-contracts": "0.0.0-alpha3",
    "@specron/cli": "^0.3.0",
    "@specron/spec": "^0.3.0",
    "solc": "^0.4.25",
    "ts-node": "^7.0.1",
    "typescript": "^3.1.1",
    "web3": "^1.0.0-beta.36"
  }
}
