export enum XcertCreateProxyAbilities {
  MANAGE_ABILITIES = 1,
  EXECUTE = 2,
}

export enum TokenTransferProxyAbilities {
  MANAGE_ABILITIES = 1,
  EXECUTE = 2,
}

export enum NFTokenTransferProxyAbilities {
  MANAGE_ABILITIES = 1,
  EXECUTE = 2,
}

export enum NFTokenSafeTransferProxyAbilities {
  MANAGE_ABILITIES = 1,
  EXECUTE = 2,
}

export enum XcertUpdateProxyAbilities {
  MANAGE_ABILITIES = 1,
  EXECUTE = 2,
}