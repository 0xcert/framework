/**
 * Order gateway proxy ids.
 */
export enum ActionsGatewayProxy {
  XCERT_CREATE = 0,
  TOKEN_TRANSFER = 1,
  NFTOKEN_TRANSFER = 2,
  NFTOKEN_SAFE_TRANSFER = 3,
  XCERT_UPDATE = 4,
  MANAGE_ABILITIES = 5,
}
