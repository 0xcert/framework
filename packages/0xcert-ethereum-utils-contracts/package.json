{
  "name": "@0xcert/ethereum-utils-contracts",
  "description": "Common utilities.",
  "version": "0.0.0-alpha7",
  "scripts": {
    "clean": "rm -Rf ./build",
    "build": "npm run clean && npx specron compile",
    "sandbox": "npx specron sandbox",
    "test": "rm -rf build && npx specron compile && npx specron test"
  },
  "specron": {
    "compiler": {
      "build": "./build",
      "match": [
        "./src/**/*.sol"
      ]
    },
    "test": {
      "port": 8505,
      "match": [
        "./src/tests/**/*.test.ts"
      ]
    },
    "require": [
      "ts-node/register"
    ]
  },
  "license": "MIT",
  "devDependencies": {
    "@specron/cli": "^0.5.0",
    "@specron/spec": "^0.5.0",
    "solc": "^0.5.1",
    "ts-node": "^7.0.1",
    "typescript": "^3.1.1",
    "web3": "^1.0.0-beta.36"
  }
}
