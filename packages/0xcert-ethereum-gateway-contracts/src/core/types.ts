export enum ActionsGatewayAbilities {
  MANAGE_ABILITIES = 1,
  ALLOW_MANAGE_ABILITIES = 2,
  SET_PROXIES = 16,
}

export enum XcertDeployGateway {
  MANAGE_ABILITIES = 1,
  ALLOW_MANAGE_ABILITIES = 2,
  SET_PROXY = 16,
}
